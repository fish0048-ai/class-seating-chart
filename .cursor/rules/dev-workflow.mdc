---
description: 開發／修改／除錯預設流程（理解→規劃→查證→實作→驗證→修正→回報）
alwaysApply: true
---

# 開發與除錯預設流程

處理開發、修改、除錯時**自動執行**，不必等老師再說「開始」。一般問答／翻譯／純文字直接答；簡單修改用精簡流程。**當次明確指示優先於本規則**。

## 流程

1. **理解**：先讀相關程式與專案規則（含 `docs/專案現況.md`、`invariants`）。確認需求、影響範圍、完成條件。資訊夠就繼續；只在關鍵缺漏時才問。
2. **規劃**：依複雜度給簡短計畫。適用時用已安裝 Superpowers（如 systematic-debugging、verification-before-completion、brainstorming）。
3. **查證**：第三方套件／API／不確定用法 → 用 Context7 查符合專案版本的文件。
4. **實作**：沿用現有架構；正式碼在 `docs/` 與根目錄 `Code.gs`。只改任務需要的內容，保留既有變更。React／Next.js 時選用適用的 Vercel 技能（本專案目前為 vanilla JS，勿硬套 React）。
5. **驗證**：跑適當測試／型別／建置；網頁互動用 Playwright（或可用的瀏覽器工具）驗證受影響流程。改 `app.js` 後確認畫面有座位卡，不要只看截圖。
6. **修正**：失敗先找根因再改，改完重驗。缺資訊／權限／環境時清楚說明阻礙，不假裝已測過。
7. **回報**：繁體中文；簡短列出修改內容、實際驗證結果、未驗證部分。完成後依 `github-sync` commit／push。

## 工具原則

- 依任務選用，不要求每次全用。
- 工具不可用時如實說明；不得假裝已使用或已通過測試。
