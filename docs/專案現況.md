# 班級座位表 — 已完成現況（2026-08-31）

這份是**現在能用的功能與不能改壞的約定**。之後改程式請先對過這裡，避免整份壞掉、卡住沒資料、或把課表寫進成績庫。

線上：https://fish0048-ai.github.io/class-seating-chart/  
GitHub：https://github.com/fish0048-ai/class-seating-chart  
GitHub Pages 來源：倉庫的 `docs/` 資料夾（`master`）

---

## 1. 老師現在可以做什麼

必須先用 **Google 帳號**登入。

| 角色 | 帳號 | 能做的事 |
| --- | --- | --- |
| 教師 | 目前只有 `chunhsinkuo@kcis.hc.edu.tw` | 加扣分、拖座位、抽籤、分組、實驗室、教師模式（名單／課表／成績／統計／設定） |
| 檢視 | 其他任何 Google 帳號 | 只能看上課／實驗室座位表與排行，沒有教師模式，不能改資料 |

上課模式：

- 選班級（801、802、803、804、範例班）
- 加分／扣分（1、2、5），可復原
- 排行只顯示**有加分**的同學
- 抽籤（可本堂不重複）
- 分組：每組人數、隨機／手動；小組分會加進每位組員的平時加扣
- 小組扣分時請先點那位同學再扣整組，成績統計才會記下「因誰」

實驗室模式：

- 固定 6 組，座位分六個區塊
- 可依座號或隨機分成六組；拖到別區換組
- 加扣預設套用整組（與上課分組是兩套資料，互不覆蓋）

教師模式分頁：名單、課表、成績表、成績統計表（含小組扣分因誰）、統計圖表、設定與上傳。

晚上 10 點會自動把當天加扣分存進日期紀錄，分數歸零開始下一天。

---

## 2. 兩份試算表，不要搞混

| 用途 | 網址 | 能不能寫入 |
| --- | --- | --- |
| 成績／座位資料庫 | https://docs.google.com/spreadsheets/d/1AES93Jv8l65YI2LQ-scVRPqYSLFxtVOD-UqIU99gQSA/edit | 會寫入（名單、分數、雲端 JSON） |
| 上課課表 | https://docs.google.com/spreadsheets/d/13VrWBx6hoKpUON_JNxIrynH_gyRV8HnhUt0MMscjkWg/edit | **只顯示，禁止寫成績或名單** |

Apps Script 綁的是**成績庫**那份。課表 ID：`13VrWBx6hoKpUON_JNxIrynH_gyRV8HnhUt0MMscjkWg`。

雲端 API（網頁應用程式 `/exec`）：

`https://script.google.com/macros/s/AKfycbzd4_bHa2bOiyltufRBD72Fw4s7Wgn68mVTWB95VDNoUYNXe7iPYl5z5WwuoKCv7nWdgA/exec`

改 `Code.gs` 後必須在試算表「擴充功能 → Apps Script」**全部貼上、儲存、部署 → 新增部署作業**（網頁應用程式、執行身分「我」、對象「任何人」）。只改 GitHub **不會**更新後端。

---

## 3. 真正會跑的檔案（只改這些）

GitHub Pages 與上課用的是 `docs/`，不是根目錄舊檔。

| 檔案 | 負責 |
| --- | --- |
| `docs/index.html` | 畫面結構、登入閃道 |
| `docs/styles.css` | 樣式、檢視模式隱藏編輯鈕 |
| `docs/config.js` | API、兩份試算表、Google 用戶端 ID、教師信箱 |
| `docs/cloud.js` | 雲端 getStore／putStore（POST + idToken） |
| `docs/api.js` | 前端末地 store、分組／實驗室、加扣、成績 |
| `docs/auth.js` | Google 登入、教師／檢視角色 |
| `docs/app.js` | 操作與畫面 |
| `Code.gs` | Apps Script 後端：讀寫雲端、驗證 Google、只有教師能寫 |

根目錄 `Index.html`、`座位表.html`、`JavaScript.html` 等是舊稿，**不要當正式畫面改**。

載入順序必須是：

`config.js` → `cloud.js` → `api.js` → `auth.js` → `app.js` → Google GSI → xlsx

---

## 4. 資料怎麼存

主資料是一整份 JSON store，寫在成績庫工作表「雲端資料」（分段，每段約 45000 字）。

結構大意：

```
store.classes[班名] = {
  className, rows, cols, version, updatedAt,
  students: [{ seatNo, name, score, row, col, note }],
  groups: { size, assign: { 座號: 組號 }, scores: { 組號: 分 } },
  lab:    { assign: { 座號: 1~6 }, scores: { "1"~"6": 分 } }
}
store.history[]   操作紀錄（含 groupId、seatNos、causeSeatNo、causeName、lab）
store.daily{}     每天加扣結算
store.grades{}    成績簿
store.scoreDate   目前加扣所屬日期
store.updatedAt
```

`api.js` 的 `normalize()` **必須保留 `groups` 與 `lab`**，否則存檔會丟掉分組。

存檔：教師操作後約 400ms debounce 推雲端（`putStore`）。讀取：`getStore` 用 POST，並帶 Google `idToken`。

---

## 5. 登入與權限（已接上）

- Google OAuth 用戶端 ID（網頁應用程式）：  
  `556988999591-tc3b8orn6ov3guinfiq5p0c9u6g0n802.apps.googleusercontent.com`
- 授權 JavaScript 來源必須包含：`https://fish0048-ai.github.io`（本機再加 `http://127.0.0.1:8767`）
- 教師信箱寫在 `docs/config.js` 的 `teacherEmails` **以及** `Code.gs` 的 `TEACHER_EMAILS`，兩邊要一起改
- 前端：`GoogleAuth.isTeacher()` 才顯示教師模式與編輯鈕
- 後端：`getStore` 要已登入；`putStore` 與所有寫入動作要教師 token（`oauth2.googleapis.com/tokeninfo`）
- 舊的教師密碼已移除，不要加回來

未把最新 `Code.gs` 新增部署前，後端可能還沒擋寫入；部署後才算真正鎖住。

---

## 6. 改程式時絕對不要做的事

1. **不要在 `renderLabBoard`／每次重繪時呼叫存檔**（以前會無限 `saveLab`，全班卡在「正在載入」）。進入實驗室只在第一次缺分組時 `persistLab` 一次。
2. **`highlightDropTarget` 等函式不要多一個 `}`**。`app.js` 括號一錯，整份 JS 不跑，畫面永遠「正在載入」、沒有班級。改完請在瀏覽器確認有座位卡。
3. **課表試算表只讀**，不要寫入成績、名單、加扣。
4. **抽籤加權不要寫進畫面、README、或跟老師說明的文字。**
5. **學生真實名單不要 commit 進公開 git**（不要把填好的 CSV／個資推進 GitHub）。
6. 不要改 git config、不要 `--force` push。
7. 不要把 `.env`、憑證當一般檔案 commit。OAuth 用戶端 ID 本來就在前端公開，可以留在 `config.js`。
8. 檢視帳號即使改了前端，也不該能 `putStore` 成功（後端必須檢查教師）。
9. `saveStore` 對檢視帳號直接 return，不要把本機亂改推上雲端。

---

## 7. 已知已修好的坑（不要再犯）

- 實驗室模式多一個 `}` → 整站無資料：已修。
- 實驗室重繪時一直存檔打掛 Apps Script：已改成重繪只補分組、不存檔。
- `normalize` 曾丟掉 `groups`：現在會保存 groups／lab。
- 載入改為 POST `getStore`，逾時會出現「重新載入」，不要只靠 JSONP。

---

## 8. 改完要驗證什麼

1. 教師 Google 登入後看得到 801～804、座位卡、能加分。
2. 其他 Google 帳號登入：沒有教師模式、沒有加扣／抽籤／分組工具列。
3. 實驗室六組能顯示，不會一直「正在存到雲端」。
4. 教師模式課表能開，且不會改到課表試算表。
5. 有改 `Code.gs` 時，提醒老師重新貼上並**新增部署**。
6. 依倉庫規則：用繁體中文 commit（`<type>: 主旨`）並 `git push origin master`。
